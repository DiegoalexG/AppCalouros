import React from 'react';
import { Text, View, Button, AppRegistry } from 'react-native';

const App = () => {
  return(
    <View>
      <Text>Vai ter um app depois aqui</Text>
    </View>
  );
};

AppRegistry.registerComponent('App', () => App);